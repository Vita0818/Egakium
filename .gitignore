# SwiftPM
.build/
.swiftpm/

# Keep the root lockfile tracked so audited renderer dependencies stay exact.

# Xcode
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
DerivedData/
dist/
*.xcuserstate

# Generated by XcodeGen (run `make app` / `xcodegen generate` to recreate)
Egakium.xcodeproj/

# macOS
.DS_Store

# Egakium local runtime/worktree state
.egakium/

# Egakium pre-migration Chromium / CEF assets retained outside the Egakium baseline
/Chromium/
/.deps/
/build/

# Secrets / local config (never commit)
*.env
.env
secrets.json
Config.local.*
