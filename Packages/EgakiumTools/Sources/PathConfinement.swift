// PathConfinement moved to EgakiumCore so EgakiumPermission can also use it
// without depending on EgakiumTools. Tools reference `PathConfinement` via
// `import EgakiumCore`. This file is intentionally left as a pointer only.
